name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-linux:
    name: Build Linux (x86_64)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-musl

      - name: Install musl tools
        run: sudo apt-get update && sudo apt-get install -y musl-tools

      - name: Build
        run: cargo build --release --target x86_64-unknown-linux-musl
        env:
          RUSTFLAGS: '-C target-feature=+crt-static'

      - name: Strip binary
        run: strip target/x86_64-unknown-linux-musl/release/systemair-save-tools

      - name: Rename binary
        run: |
          mkdir -p artifacts
          cp target/x86_64-unknown-linux-musl/release/systemair-save-tools artifacts/systemair-save-tools-linux-x86_64

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: linux-x86_64
          path: artifacts/systemair-save-tools-linux-x86_64

  build-windows:
    name: Build Windows (x86_64)
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Build
        run: cargo build --release --target x86_64-pc-windows-msvc
        env:
          RUSTFLAGS: '-C target-feature=+crt-static'

      - name: Rename binary
        shell: bash
        run: |
          mkdir -p artifacts
          cp target/x86_64-pc-windows-msvc/release/systemair-save-tools.exe artifacts/systemair-save-tools-windows-x86_64.exe

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-x86_64
          path: artifacts/systemair-save-tools-windows-x86_64.exe

  build-macos-intel:
    name: Build macOS (x86_64)
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-apple-darwin

      - name: Build
        run: cargo build --release --target x86_64-apple-darwin
        env:
          RUSTFLAGS: '-C link-arg=-s'

      - name: Rename binary
        run: |
          mkdir -p artifacts
          cp target/x86_64-apple-darwin/release/systemair-save-tools artifacts/systemair-save-tools-macos-x86_64

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: macos-x86_64
          path: artifacts/systemair-save-tools-macos-x86_64

  build-macos-arm:
    name: Build macOS (ARM64)
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin

      - name: Build
        run: cargo build --release --target aarch64-apple-darwin
        env:
          RUSTFLAGS: '-C link-arg=-s'

      - name: Rename binary
        run: |
          mkdir -p artifacts
          cp target/aarch64-apple-darwin/release/systemair-save-tools artifacts/systemair-save-tools-macos-arm64

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: macos-arm64
          path: artifacts/systemair-save-tools-macos-arm64

  create-release:
    name: Create Release
    needs: [build-linux, build-windows, build-macos-intel, build-macos-arm]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Display structure of downloaded files
        run: ls -R artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            artifacts/linux-x86_64/systemair-save-tools-linux-x86_64
            artifacts/windows-x86_64/systemair-save-tools-windows-x86_64.exe
            artifacts/macos-x86_64/systemair-save-tools-macos-x86_64
            artifacts/macos-arm64/systemair-save-tools-macos-arm64
          draft: false
          prerelease: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
